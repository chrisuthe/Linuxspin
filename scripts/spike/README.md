Measurement probes behind the "UI shell" section of `docs/ARCHITECTURE.md`. Referenced by nothing —
not the solution, not CI, not the Makefile — so they build only when run by hand: `dotnet run --project scripts/spike/ShellSpike -- <mode>`.
